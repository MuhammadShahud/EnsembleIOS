import React from 'react';
import { View, Text } from 'react-native';
import { style } from './teamsStyle';

export default function Teams() {
  return (
    <View style={style.innerView}>
      <Text>Teams</Text>
     </View>
  );
}
